export enum LoginStatus {
  SUCCESS = 'SUCCESS',
  // NOT_CONFIRMED = 'NOT_CONFIRMED',
  UNAUTHORIZED = 'UNAUTHORIZED',
  FAIL = 'FAIL',
}

export enum Os {
  WINDOWS = 'WINDOWS',
  UBUNTU = 'UBUNTU',
  CENTOS = 'CENTOS',
}
